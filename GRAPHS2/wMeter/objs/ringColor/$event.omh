On evClick     ;; Event Parameters - pRow( Itemreference )
	;  $addring can only be called in the evPreLayout
	Do $cinst.$objs.meterGraph.$dispose()
	
On evRMouseDown
	Set reference lObjItemRef to $cobj
	Do $cwind.$setItemRef(lObjItemRef)